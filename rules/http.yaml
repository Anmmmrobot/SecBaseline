- id: HTTP-001
  name: Check HSTS header
  module: http
  type: config_equals
  key: has_hsts
  expected: true
  profiles: [basic, strict]
  severity: medium
  recommendation: Set Strict-Transport-Security header.

- id: HTTP-002
  name: Check CSP header
  module: http
  type: config_equals
  key: has_csp
  expected: true
  profiles: [basic, strict]
  severity: medium
  recommendation: Set Content-Security-Policy header.

- id: HTTP-003
  name: Check X-Frame-Options header
  module: http
  type: config_equals
  key: has_x_frame_options
  expected: true
  profiles: [basic, strict]
  severity: medium
  recommendation: Set X-Frame-Options header.

- id: HTTP-004
  name: Check X-Content-Type-Options header
  module: http
  type: config_equals
  key: has_x_content_type_options
  expected: true
  profiles: [basic, strict]
  severity: medium
  recommendation: "Set X-Content-Type-Options: nosniff."

- id: HTTP-005
  name: Check Server header exposure
  module: http
  type: config_equals
  key: server_exposed
  expected: false
  profiles: [basic, strict]
  severity: low
  recommendation: Hide or minimize Server response header details.
